node_modules/
dist/
coverage/
.env
.env.*
!.env.example

# Untracked local project notes — private session state / machine-specific deploy
# pointers kept out of the public case-study repo.
CLAUDE.local.md
HANDOFF.md
playwright-report/
test-results/
*.log

# TanStack Router generated route tree + the generator's temp dir (regenerated by the
# vite plugin / `tsr generate`)
routeTree.gen.ts
.tanstack/
.DS_Store
